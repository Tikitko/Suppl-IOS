import Foundation

final class Audio {
    /*
    public static func search(api: API, ikey: Int, akey: Int, query: String, offset: Int = 0, dataReport: @escaping (NSError?, AudioSearchData?) -> ()) {
        var query = ["method": "audio.search", "query": query, "offset": String(offset)]
        User.authPartAdd(query: &query, ikey, akey)
        api.method(query: query, dataReport: dataReport) { data in
            return AudioSearchData.parse(data)
        }
    }
    
    public static func get(api: API, ikey: Int, akey: Int, ids: String, dataReport: @escaping (NSError?, AudioListData?) -> ()) {
        var query = ["method": "audio.get", "ids": ids]
        User.authPartAdd(query: &query, ikey, akey)
        api.method(query: query, dataReport: dataReport) { data in
            return AudioListData.parse(data)
        }
    }
 */
}
